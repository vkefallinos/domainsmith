# Vitest

Fast unit testing with native ESM support.

**Features:**
- Jest-compatible API
- Out-of-box TypeScript/JSX
- Watch mode with HMR
- Built-in code coverage

Use `describe`, `it`, `expect` as in Jest. Imports work natively without transforms.